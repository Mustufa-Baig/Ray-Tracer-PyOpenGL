#version 330 core

in vec3 fragmentColor;

out vec4 color;

uniform vec2 u_resolution;

float hitSphere(vec3 center, float radius, vec3 ro, vec3 rd) {
    vec3 oc = ro - center;
    
    float b = dot(oc, rd);
    float c = dot(oc, oc) - radius * radius;
    float discriminant = b * b - c;
    
    if (discriminant > 0.0) {
        return -b - sqrt(discriminant);
    }
    
    return -1.0;
}

void main()
{
	vec2 uv= gl_FragCoord.xy / u_resolution.xy;
	uv=(uv*2.0) -1.0;
	uv.x *= u_resolution.x/u_resolution.y;


	vec3 cam_pos = vec3(0.0, 0.0, -3.0); 
	vec3 cam_dir = normalize(vec3(uv, 1.0)); 

	vec3 sphere_pos = vec3(0.0, 0.0, 0.0);
	float sphere_rad = 1.0;

	
	float t = hitSphere(sphere_pos, sphere_rad, cam_pos, cam_dir);

	if (t > 0.0) {
		color = vec4(1.0, 0.0, 0.0, 1.0);
	} else {
		color = vec4(0.1, 0.1, 0.1, 1.0);
	}
}